# Notas pessoais (não versionar)
notas-pessoais/
*.local.md
.obsidian/

# Arquivos do harness, ligados por symlink ao perfil. Sem isto um `git add -A`
# os versiona, e o build tenta renderizar um .md que não existe no remoto — 404
# e publicação quebrada.
CLAUDE.md
CLAUDE.local.md
.mcp.json
.claudio

# Sistema
.DS_Store
Thumbs.db

# Editores
.vscode/
.idea/
*.swp

# Ambientes e segredos
.env
*.pem
*.key
credentials*
venv/
__pycache__/

# Worktrees criados pelo harness
.claude/worktrees/

# Site gerado pelo build (tools/build-site.py)
_site/

# Artefatos locais do Impeccable: capturas de revisão e log da página
# de decisão. O que fica versionado é o contrato e o design system.
.impeccable/review/
.impeccable/questions/
